package "nova/math"

public class NumericTree {
    NumericOperation root
    
    public construct(String statement) => root = new NumericOperation(statement)
    
    public toString() => root.toString()
}