package "nova/star"

trait UIAction extends UIComponent {
    Array<actionFunc()> listeners = new Array()
    
    public onAction() {
        // listeners.forEach({ _() })
    }
    
    public addActionListener(func()) {
        listeners.add(func)
    }
}