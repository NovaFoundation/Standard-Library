package "nova/star"

class Button extends Panel {
    public construct(Int x = 0, Int y = 0, Int width = 0, Int height = 0) =>
        super(x, y, width, height)
    
    public draw() {
        external c {
            RECT r;
            
            r.left   = #{x};
            r.right  = #{x} + #{width};
            r.top    = #{y};
            r.bottom = #{y} + #{height};
            
            DrawFrameControl(*#{window.hdc}, &r, DFC_BUTTON, DFCS_BUTTONPUSH);
        }
    }
}