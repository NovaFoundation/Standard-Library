package "nova/exception"

class Exception {
    visible String message
    
    public construct(String message = "") => this.message = message
    
    public toString() => this.class.name + (message != null ? ": #message" : "")
}